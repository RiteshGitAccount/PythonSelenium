pytest
selenium
webdriver-manager # For downloading respective browser driver as per local system installed version
allure-pytest # For allure framework and reporting
openpyxl # For handling Excel files
pytest-ordering # For defining order of the test cases
pytest-xdist # For parallel execution
pytest-depends # For dependacy test case execution